# Production Hub UI

The v1.6.0 interface binds Device, Scene, Automation, Learning, Cloud and Activity models to one 800x480 touchscreen experience. Local control remains clearly available during cloud outages. UI actions use the shared command-dispatch path and never own RF, IR, storage or network lifecycles.
