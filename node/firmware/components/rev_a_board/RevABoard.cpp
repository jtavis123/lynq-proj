#include "RevABoard.h"
namespace lynq::node {
RevABoardStatus RevABoard::selfTest() {
    // Exact Port ABC GPIO mappings are intentionally supplied only after
    // verifying the received AtomS3-Lite and Port ABC hardware revision.
    return {};
}
}
