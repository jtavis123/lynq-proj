import test from'node:test';import assert from'node:assert/strict';import{createState,toggleDevice,runScene,setCloud}from'../src/model.js';import{bannerMessage}from'../src/render.js';test('toggles available device',()=>assert.equal(toggleDevice(createState(),'tv').devices[0].active,true));test('blocks unavailable device',()=>assert.equal(toggleDevice(createState(),'patio').devices[2].active,false));test('records scene activity',()=>assert.equal(runScene(createState(),'movie').activity[0].title,'Movie Night'));test('shows local control banner',()=>assert.match(bannerMessage(setCloud(createState(),false)),/local control/i));
